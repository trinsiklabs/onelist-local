# Onelist Local Configuration
# Copy to .env.local and fill in the values
#
# Only TWO things required to get started:
# 1. SECRET_KEY_BASE - generate with: openssl rand -hex 64
# 2. OPENAI_API_KEY - from https://platform.openai.com/api-keys

# =============================================================================
# REQUIRED
# =============================================================================

# Generate with: openssl rand -hex 64
SECRET_KEY_BASE=

# From https://platform.openai.com/api-keys
# Used for embeddings (semantic search) and Reader agent
OPENAI_API_KEY=

# Your email - creates your initial account
INITIAL_USER_EMAIL=you@example.com

# =============================================================================
# OPTIONAL
# =============================================================================

# Port to run on (default: 4000)
# PORT=4000

# Hostname (default: localhost)
# PHX_HOST=localhost
