#!/bin/sh

# Sets environment variables for production releases
# This file is sourced before the application starts

# Configure ERTS for better memory management
export ERL_CRASH_DUMP_SECONDS=0
export ERL_FULLSWEEP_AFTER=10

# Enable shell history in IEx
export ERL_AFLAGS="-kernel shell_history enabled"
